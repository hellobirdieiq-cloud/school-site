Static study site served at https://school.stridin.app via GitHub Pages.
Add a page: drop a self-contained .html file in the repo root and link it from index.html.
Publish: git add -A && git commit -m "update" && git push
